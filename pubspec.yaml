name: flutter_nestjs_paginate
description: "A library providing a controllable paginated view builder compatible with `nestjs-paginate`"
version: 0.0.1
homepage: https://github.com/mitryp/flutter_nestjs_paginate

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.7
  freezed: ^2.4.6
  json_serializable: ^6.7.1
  test_utils:
    git:
      url: https://github.com/mitryp/flutter_utils
      path: packages/test_utils


flutter:
